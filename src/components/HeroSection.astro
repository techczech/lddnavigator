---
interface HeroItem {
  title: string;
  icon: string;
  href: string;
  color: 'orange' | 'green' | 'blue';
}

const items: HeroItem[] = [
  { title: 'What are the signs?', icon: '👁️', href: '/behaviours', color: 'orange' },
  { title: 'What do I do?', icon: '📋', href: '/strategies', color: 'green' },
  { title: 'Who do I tell?', icon: '💬', href: '/resources', color: 'blue' },
];

const bgColors = {
  orange: 'bg-ldd-orange hover:bg-ldd-orange-dark',
  green: 'bg-ldd-green hover:bg-ldd-green-dark',
  blue: 'bg-ldd-blue hover:bg-ldd-blue-dark',
};
---

<section class="bg-gray-100 py-12">
  <div class="max-w-5xl mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {items.map(item => (
        <a
          href={item.href}
          class:list={[
            'group flex flex-col items-center p-8 rounded-lg text-white transition-all card-hover',
            bgColors[item.color],
          ]}
        >
          <div class="text-6xl mb-4 group-hover:scale-110 transition-transform">{item.icon}</div>
          <span class="text-lg font-semibold text-center">{item.title}</span>
          <span class="mt-2 text-sm opacity-80 group-hover:opacity-100">Learn more &rarr;</span>
        </a>
      ))}
    </div>
  </div>
</section>
